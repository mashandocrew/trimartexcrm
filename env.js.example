// Copiá este archivo a env.js (sin el .example) para correr el CRM en local.
// env.js está en .gitignore — nunca se commitea con valores reales.
// SUPABASE_URL y SUPABASE_ANON_KEY no son secretos (la protección real es RLS),
// pero igual los mantenemos fuera del repo para no hardcodearlos en el HTML.
window.__ENV = {
  SUPABASE_URL: "https://yvjxftmfjoxajjryxabo.supabase.co",
  SUPABASE_ANON_KEY: "REEMPLAZAR_CON_LA_ANON_KEY"
};
